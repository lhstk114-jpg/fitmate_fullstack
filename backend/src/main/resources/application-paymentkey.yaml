kakao:
  admin-key: ${KAKAO_ADMIN_KEY}
  map:
    kakao-key: ${KAKAO_MAP_KEY}

# 구글 번역 API
google:
  translate:
    api-key: ${GOOGLE_TRANSLATE_API_KEY}

# 운동 루틴 API
exercisedb:
  api:
    key: ${EXERCISEDB_API_KEY}

# 날씨 API
openweather:
  base-url: https://api.openweathermap.org/data/2.5/weather
  api-key: ${OPENWEATHER_API_KEY}
